<Window x:Class="HaCreator.GUI.Skill.SkillEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:HaCreator.GUI.Skill"
        Title="{local:SkillEditorText Title}" Width="1600" Height="900" MinWidth="1280" MinHeight="720"
        WindowStartupLocation="CenterOwner" Background="{DynamicResource HareCanvasBrush}"
        FontFamily="Segoe UI" FontSize="13" UseLayoutRounding="True" SnapsToDevicePixels="True"
        Closing="Window_Closing" PreviewKeyDown="Window_PreviewKeyDown">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/HaSharedLibrary;component/GUI/Themes/HarepackerTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
            <DrawingBrush x:Key="CheckerBrush" TileMode="Tile" Viewport="0,0,20,20" ViewportUnits="Absolute">
                <DrawingBrush.Drawing><DrawingGroup>
                    <GeometryDrawing Brush="#FFF8FAFC" Geometry="M0,0 H20 V20 H0 Z"/>
                    <GeometryDrawing Brush="#FFE7EBF0" Geometry="M0,0 H10 V10 H0 Z M10,10 H20 V20 H10 Z"/>
                </DrawingGroup></DrawingBrush.Drawing>
            </DrawingBrush>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="58"/><RowDefinition Height="*"/><RowDefinition Height="30"/></Grid.RowDefinitions>
        <Border Grid.Row="0" Background="{DynamicResource HareSurfaceBrush}" BorderBrush="{DynamicResource HareBorderBrush}" BorderThickness="0,0,0,1">
            <Grid Margin="12,0"><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <WrapPanel VerticalAlignment="Center">
                    <Button x:Name="saveButton" Style="{StaticResource HarePrimaryButtonStyle}" MinWidth="82" Height="34" Content="{local:SkillEditorText Save}" Click="Save_Click"/>
                    <Button x:Name="undoButton" Margin="8,0,0,0" Height="34" Content="{local:SkillEditorText Undo}" Click="Undo_Click"/>
                    <Button x:Name="redoButton" Margin="4,0,0,0" Height="34" Content="{local:SkillEditorText Redo}" Click="Redo_Click"/>
                    <Separator Margin="8,7"/>
                    <Button Height="34" Content="{local:SkillEditorText ValidateCurrent}" Click="ValidateCurrent_Click"/>
                    <Button Margin="4,0,0,0" Height="34" Content="{local:SkillEditorText ValidateAll}" Click="ValidateAll_Click"/>
                    <Separator Margin="8,7"/>
                    <Button Height="34" Content="{local:SkillEditorText Duplicate}" Click="Duplicate_Click"/>
                    <Button Margin="4,0,0,0" Height="34" Content="{local:SkillEditorText NewSkill}" Click="NewSkill_Click"/>
                    <Button Margin="4,0,0,0" Height="34" Content="{local:SkillEditorText RenameMoveSkill}" Click="RenameMoveSkill_Click"/>
                    <Button Margin="4,0,0,0" Height="34" Style="{StaticResource HareDangerButtonStyle}" Content="{local:SkillEditorText DeleteSkill}" Click="DeleteSkill_Click"/>
                </WrapPanel>
                <TextBlock Grid.Column="1" Margin="18,0" VerticalAlignment="Center" Foreground="{DynamicResource HareMutedTextBrush}" Text="{local:SkillEditorText Purpose}" TextTrimming="CharacterEllipsis"/>
                <Border Grid.Column="2" VerticalAlignment="Center" Padding="10,6" Background="#EEF2F7" CornerRadius="5">
                    <StackPanel Orientation="Horizontal"><TextBlock Text="{local:SkillEditorText Source}" Foreground="{DynamicResource HareMutedTextBrush}"/><TextBlock x:Name="sourceText" Margin="8,0,0,0" FontWeight="SemiBold"/></StackPanel>
                </Border>
            </Grid>
        </Border>

        <Grid Grid.Row="1"><Grid.ColumnDefinitions><ColumnDefinition Width="300" MinWidth="235"/><ColumnDefinition Width="5"/><ColumnDefinition Width="*" MinWidth="560"/><ColumnDefinition Width="5"/><ColumnDefinition Width="370" MinWidth="300"/></Grid.ColumnDefinitions>
            <Border Grid.Column="0" Margin="6" Style="{StaticResource HareCardStyle}">
                <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="3*"/><RowDefinition Height="Auto"/><RowDefinition Height="4*"/></Grid.RowDefinitions>
                    <TextBox x:Name="searchBox" ToolTip="{local:SkillEditorText Search}" TextChanged="Filter_Changed"/>
                    <ComboBox x:Name="scopeComboBox" Grid.Row="1" Margin="0,8" SelectionChanged="Filter_Changed">
                        <ComboBoxItem Tag="All" Content="{local:SkillEditorText All}" IsSelected="True"/><ComboBoxItem Tag="Player" Content="{local:SkillEditorText Player}"/><ComboBoxItem Tag="Special" Content="{local:SkillEditorText Special}"/>
                    </ComboBox>
                    <WrapPanel Grid.Row="2" Margin="0,0,0,6">
                        <ComboBox x:Name="activityFilter" Width="92" SelectionChanged="Filter_Changed"><ComboBoxItem Tag="All" Content="{local:SkillEditorText All}" IsSelected="True"/><ComboBoxItem Tag="Active" Content="{local:SkillEditorText Active}"/><ComboBoxItem Tag="Passive" Content="{local:SkillEditorText Passive}"/></ComboBox>
                        <CheckBox x:Name="hiddenFilter" Margin="8,0,0,0" VerticalAlignment="Center" Content="{local:SkillEditorText Hidden}" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                        <CheckBox x:Name="warningFilter" Margin="8,0,0,0" VerticalAlignment="Center" Content="{local:SkillEditorText Warnings}" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                        <CheckBox x:Name="propertySearchCheckBox" Margin="0,4,0,0" VerticalAlignment="Center" Content="{local:SkillEditorText SearchProperties}" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                    </WrapPanel>
                    <ListBox x:Name="bookList" Grid.Row="3" DisplayMemberPath="DisplayName" SelectionChanged="Book_SelectionChanged" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"><ListBox.GroupStyle><GroupStyle><GroupStyle.HeaderTemplate><DataTemplate><TextBlock Margin="0,6,0,2" FontWeight="SemiBold" Foreground="{DynamicResource HareMutedTextBrush}" Text="{Binding Name}"/></DataTemplate></GroupStyle.HeaderTemplate></GroupStyle></ListBox.GroupStyle></ListBox>
                    <TextBlock Grid.Row="4" Margin="0,12,0,6" Style="{StaticResource HareSectionTitleStyle}" Text="{local:SkillEditorText Skills}"/>
                    <ListBox x:Name="skillList" Grid.Row="5" SelectionChanged="Skill_SelectionChanged" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"><ListBox.ItemTemplate><DataTemplate><Grid Margin="0,2"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="{Binding DisplayName}" TextTrimming="CharacterEllipsis"/><TextBlock Foreground="{DynamicResource HareMutedTextBrush}" FontSize="11"><Run Text="{Binding Metadata.Activity}"/><Run Text="  Lv. "/><Run Text="{Binding Metadata.MaxLevel}"/></TextBlock></StackPanel><StackPanel Grid.Column="1" Orientation="Horizontal"><TextBlock Foreground="{DynamicResource HareWarningBrush}" Text="!" Visibility="{Binding Metadata.HasWarnings, Converter={StaticResource BooleanToVisibilityConverter}}"/><TextBlock Margin="4,0,0,0" Text="H" Visibility="{Binding Metadata.IsHidden, Converter={StaticResource BooleanToVisibilityConverter}}"/></StackPanel></Grid></DataTemplate></ListBox.ItemTemplate></ListBox>
                </Grid>
            </Border>
            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="Transparent"/>

            <Grid Grid.Column="2" Margin="0,6"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                <Border Style="{StaticResource HareCardStyle}" Margin="0,0,0,6">
                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <StackPanel><TextBlock x:Name="skillTitle" FontSize="20" FontWeight="SemiBold" Text="{local:SkillEditorText NoSelection}"/><TextBlock x:Name="skillPathText" Margin="0,4,0,0" Foreground="{DynamicResource HareMutedTextBrush}"/></StackPanel>
                        <Border Grid.Column="1" x:Name="rawBadge" Background="#EFF6FF" BorderBrush="#BFDBFE" BorderThickness="1" CornerRadius="4" Padding="8,4"><TextBlock Foreground="#1D4ED8" Text="{local:SkillEditorText PreservedRaw}"/></Border>
                    </Grid>
                </Border>
                <TabControl Grid.Row="1" x:Name="workspaceTabs" SelectionChanged="WorkspaceTabs_SelectionChanged">
                    <TabItem Header="{local:SkillEditorText Overview}"><ScrollViewer VerticalScrollBarVisibility="Auto"><StackPanel Margin="16">
                        <Border Style="{StaticResource HareCardStyle}"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="120"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                            <StackPanel><Border Width="96" Height="96" Background="{StaticResource CheckerBrush}" BorderBrush="{DynamicResource HareBorderBrush}" BorderThickness="1"><Image x:Name="iconImage" Stretch="None"/></Border><WrapPanel Margin="0,6,0,0"><Button Content="{local:SkillEditorText ImportIcon}" Click="ImportIcon_Click"/><Button Margin="4,0,0,0" Content="{local:SkillEditorText ExportIcon}" Click="ExportIcon_Click"/></WrapPanel></StackPanel>
                            <Grid Grid.Column="1" Margin="16,0,0,0"><Grid.ColumnDefinitions><ColumnDefinition Width="125"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                                <TextBlock Text="{local:SkillEditorText SkillId}" Foreground="{DynamicResource HareMutedTextBrush}"/><TextBlock x:Name="overviewId" Grid.Column="1" FontWeight="SemiBold"/>
                                <TextBlock Grid.Row="1" Text="{local:SkillEditorText Book}" Foreground="{DynamicResource HareMutedTextBrush}"/><TextBlock x:Name="overviewBook" Grid.Row="1" Grid.Column="1"/>
                                <TextBlock Grid.Row="2" Text="{local:SkillEditorText Name}" Foreground="{DynamicResource HareMutedTextBrush}"/><TextBox x:Name="overviewName" Grid.Row="2" Grid.Column="1" IsReadOnly="True" LostFocus="StringField_LostFocus"/>
                                <TextBlock Grid.Row="3" Text="{local:SkillEditorText Description}" Foreground="{DynamicResource HareMutedTextBrush}"/><TextBox x:Name="overviewDescription" Grid.Row="3" Grid.Column="1" MinHeight="90" AcceptsReturn="True" TextWrapping="Wrap" IsReadOnly="True" LostFocus="StringField_LostFocus"/>
                            </Grid>
                        </Grid></Border>
                        <Border Margin="0,10,0,0" Style="{StaticResource HareCardStyle}"><StackPanel><TextBlock Style="{StaticResource HareSectionTitleStyle}" Text="{local:SkillEditorText RenderedDescription}"/><TextBlock x:Name="renderedDescription" Margin="0,6,0,0" TextWrapping="Wrap"/></StackPanel></Border>
                        <Border Margin="0,10,0,0" Style="{StaticResource HareCardStyle}"><Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions><StackPanel><TextBlock Foreground="{DynamicResource HareMutedTextBrush}" Text="{local:SkillEditorText Classification}"/><TextBlock x:Name="overviewClassification" FontWeight="SemiBold"/></StackPanel><StackPanel Grid.Column="1"><TextBlock Foreground="{DynamicResource HareMutedTextBrush}" Text="{local:SkillEditorText MaximumLevel}"/><TextBlock x:Name="overviewMaximum" FontWeight="SemiBold"/></StackPanel><StackPanel Grid.Column="2"><TextBlock Foreground="{DynamicResource HareMutedTextBrush}" Text="{local:SkillEditorText RequirementsFlags}"/><TextBlock x:Name="overviewFlags" TextWrapping="Wrap"/></StackPanel></Grid></Border>
                        <CheckBox x:Name="editTextCheckBox" Margin="0,14,0,0" Content="{local:SkillEditorText EditText}" Checked="EditText_Checked"/>
                        <TextBlock Margin="24,4,0,0" Foreground="{DynamicResource HareWarningBrush}" Text="{local:SkillEditorText OtherImageWarning}"/>
                        <DataGrid x:Name="stringMetadataGrid" Margin="0,8,0,0" AutoGenerateColumns="False" IsReadOnly="True" CellEditEnding="StringMetadata_CellEditEnding"><DataGrid.Columns><DataGridTextColumn Header="{local:SkillEditorText Property}" Binding="{Binding Name}" IsReadOnly="True"/><DataGridTextColumn Header="{local:SkillEditorText RawValue}" Binding="{Binding Value, UpdateSourceTrigger=Explicit}" Width="*"/></DataGrid.Columns></DataGrid>
                    </StackPanel></ScrollViewer></TabItem>

                    <TabItem Header="{local:SkillEditorText Levels}"><Grid Margin="12"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                        <StackPanel Orientation="Horizontal"><TextBlock VerticalAlignment="Center" Text="{local:SkillEditorText PreviewLevel}"/><Slider x:Name="levelSlider" Margin="12,0" Width="260" Minimum="1" Maximum="100" Value="1" TickFrequency="1" IsSnapToTickEnabled="True" ValueChanged="Level_ValueChanged"/><TextBlock x:Name="levelText" VerticalAlignment="Center" Text="1"/></StackPanel>
                        <TabControl x:Name="levelsModeTabs" Grid.Row="1" Margin="0,10,0,0"><TabItem x:Name="formulaModeTab" Header="{local:SkillEditorText FormulaMode}"><DataGrid x:Name="commonGrid" AutoGenerateColumns="False" CellEditEnding="Formula_CellEditEnding"><DataGrid.Columns><DataGridTextColumn Header="{local:SkillEditorText Property}" Binding="{Binding Name}" IsReadOnly="True"/><DataGridTextColumn Header="{local:SkillEditorText Type}" Binding="{Binding Type}" IsReadOnly="True"/><DataGridTextColumn Header="{local:SkillEditorText RawValue}" Binding="{Binding Raw, Mode=OneWay}" Width="*"/><DataGridTextColumn Header="{local:SkillEditorText Evaluated}" Binding="{Binding Evaluated}" IsReadOnly="True"/><DataGridTextColumn Header="{local:SkillEditorText Validation}" Binding="{Binding Validation}" IsReadOnly="True" Width="*"/></DataGrid.Columns></DataGrid></TabItem>
                            <TabItem x:Name="pvpModeTab" Header="{local:SkillEditorText PvpCommon}"><DataGrid x:Name="pvpGrid" AutoGenerateColumns="False" CellEditEnding="Formula_CellEditEnding"><DataGrid.Columns><DataGridTextColumn Header="{local:SkillEditorText Property}" Binding="{Binding Name}" IsReadOnly="True"/><DataGridTextColumn Header="{local:SkillEditorText Type}" Binding="{Binding Type}" IsReadOnly="True"/><DataGridTextColumn Header="{local:SkillEditorText RawValue}" Binding="{Binding Raw, Mode=OneWay}" Width="*"/><DataGridTextColumn Header="{local:SkillEditorText Evaluated}" Binding="{Binding Evaluated}" IsReadOnly="True"/></DataGrid.Columns></DataGrid></TabItem>
                            <TabItem x:Name="explicitModeTab" Header="{local:SkillEditorText ExplicitMode}"><DockPanel><WrapPanel DockPanel.Dock="Top" Margin="0,0,0,6"><Button Content="{local:SkillEditorText CopyCell}" Click="CopyExplicitCell_Click"/><Button Margin="4,0" Content="{local:SkillEditorText FillDown}" Click="FillExplicitDown_Click"/></WrapPanel><DataGrid x:Name="explicitGrid" AutoGenerateColumns="True" SelectionUnit="Cell" CellEditEnding="Explicit_CellEditEnding"/></DockPanel></TabItem></TabControl>
                    </Grid></TabItem>

                    <TabItem x:Name="visualsTab" Header="{local:SkillEditorText Visuals}"><Grid Margin="8"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="240"/></Grid.RowDefinitions><Grid.ColumnDefinitions><ColumnDefinition Width="220"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <DockPanel Grid.RowSpan="3"><Expander DockPanel.Dock="Bottom" Header="{local:SkillEditorText CustomProfile}" IsExpanded="False" Margin="0,8,0,0"><Grid Margin="4"><Grid.ColumnDefinitions><ColumnDefinition Width="70"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><Grid.RowDefinitions><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/></Grid.RowDefinitions>
                            <TextBlock Text="{local:SkillEditorText Body}"/><TextBox x:Name="bodyProfileText" Grid.Column="1" Text="00002000.img"/>
                            <TextBlock Grid.Row="1" Text="{local:SkillEditorText Head}"/><TextBox x:Name="headProfileText" Grid.Row="1" Grid.Column="1" Text="00012000.img"/>
                            <TextBlock Grid.Row="2" Text="{local:SkillEditorText Face}"/><TextBox x:Name="faceProfileText" Grid.Row="2" Grid.Column="1" Text="00020000.img"/>
                            <TextBlock Grid.Row="3" Text="{local:SkillEditorText Hair}"/><TextBox x:Name="hairProfileText" Grid.Row="3" Grid.Column="1" Text="00030000.img"/>
                            <TextBlock Grid.Row="4" Text="{local:SkillEditorText Equipment}"/><TextBox x:Name="equipmentProfileText" Grid.Row="4" Grid.Column="1" ToolTip="{local:SkillEditorText EquipmentHint}"/>
                            <Button Grid.Row="5" Grid.ColumnSpan="2" Margin="0,6,0,0" Content="{local:SkillEditorText ApplyProfile}" Click="ApplyProfile_Click"/>
                        </Grid></Expander><ListBox x:Name="visualTrackList" DisplayMemberPath="Name" SelectionChanged="VisualTrack_SelectionChanged"/></DockPanel>
                        <Border Grid.Column="1" Style="{StaticResource HareToolbarStyle}" Margin="8,0,0,6"><WrapPanel VerticalAlignment="Center">
                            <CheckBox x:Name="showCharacterCheckBox" IsChecked="True" Content="{local:SkillEditorText ShowCharacter}" Checked="PreviewOption_Changed" Unchecked="PreviewOption_Changed"/>
                            <TextBlock Margin="14,0,4,0" VerticalAlignment="Center" Text="{local:SkillEditorText Action}"/><ComboBox x:Name="actionComboBox" Width="150" SelectionChanged="PreviewOption_Changed"/>
                            <TextBlock Margin="14,0,4,0" VerticalAlignment="Center" Text="{local:SkillEditorText Facing}"/><ComboBox x:Name="facingComboBox" Width="90" SelectedIndex="0" SelectionChanged="PreviewOption_Changed"><ComboBoxItem Content="{local:SkillEditorText FacingLeft}"/><ComboBoxItem Content="{local:SkillEditorText FacingRight}"/></ComboBox>
                            <TextBlock Margin="14,0,4,0" VerticalAlignment="Center" Text="{local:SkillEditorText CharacterPreset}"/><ComboBox x:Name="presetComboBox" Width="140" SelectedIndex="0" SelectionChanged="PreviewOption_Changed"><ComboBoxItem Content="{local:SkillEditorText Male}"/><ComboBoxItem Content="{local:SkillEditorText Female}"/><ComboBoxItem Content="{local:SkillEditorText Custom}"/></ComboBox>
                            <TextBlock Margin="14,0,4,0" VerticalAlignment="Center" Text="{local:SkillEditorText Zoom}"/><Slider x:Name="zoomSlider" Width="90" Minimum="0.25" Maximum="4" Value="1" TickFrequency="0.25"/>
                            <Button Margin="4,0" Content="{local:SkillEditorText Fit}" Click="Fit_Click"/><CheckBox x:Name="onionSkinCheckBox" Content="{local:SkillEditorText OnionSkin}" Checked="OnionSkin_Changed" Unchecked="OnionSkin_Changed"/>
                        </WrapPanel></Border>
                        <Border Grid.Row="1" Grid.Column="1" Margin="8,0,0,6" Background="{StaticResource CheckerBrush}" BorderBrush="{DynamicResource HareBorderBrush}" BorderThickness="1"><Grid><ScrollViewer x:Name="previewViewport" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" PanningMode="Both"><Canvas x:Name="previewSurface" Width="900" Height="480" HorizontalAlignment="Center" VerticalAlignment="Center"><Canvas.LayoutTransform><ScaleTransform ScaleX="{Binding ElementName=zoomSlider, Path=Value}" ScaleY="{Binding ElementName=zoomSlider, Path=Value}"/></Canvas.LayoutTransform><Image x:Name="onionPreview" Stretch="None" Opacity="0.25"/><Image x:Name="characterPreview" Stretch="None"/><Image x:Name="visualPreview" Stretch="None"/></Canvas></ScrollViewer><TextBlock x:Name="actionStatus" Margin="10" VerticalAlignment="Top" HorizontalAlignment="Left" Padding="8,4" Background="#EFFFFFFF"/></Grid></Border>
                        <Grid Grid.Row="2" Grid.Column="1" Margin="8,0,0,0"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                            <WrapPanel><Button ToolTip="{local:SkillEditorText Play}" AutomationProperties.Name="{local:SkillEditorText Play}" Content="▶" Click="Play_Click"/><Button Margin="4,0" ToolTip="{local:SkillEditorText PreviousFrame}" AutomationProperties.Name="{local:SkillEditorText PreviousFrame}" Content="◀" Click="PreviousFrame_Click"/><Button ToolTip="{local:SkillEditorText NextFrame}" AutomationProperties.Name="{local:SkillEditorText NextFrame}" Content="▶" Click="NextFrame_Click"/><Button Margin="12,0,0,0" Content="{local:SkillEditorText Materialize}" Click="Materialize_Click"/><Button Margin="4,0" Content="{local:SkillEditorText Import}" Click="ImportFrame_Click"/><Button Content="{local:SkillEditorText Export}" Click="ExportFrame_Click"/></WrapPanel>
                            <WrapPanel Grid.Row="1" Margin="0,4"><Button Content="{local:SkillEditorText FrameDuplicate}" Click="DuplicateFrame_Click"/><Button Margin="4,0" Content="{local:SkillEditorText FrameDelete}" Style="{StaticResource HareDangerButtonStyle}" Click="DeleteFrame_Click"/><Button Content="{local:SkillEditorText MoveLeft}" Click="MoveFrameLeft_Click"/><Button Margin="4,0" Content="{local:SkillEditorText MoveRight}" Click="MoveFrameRight_Click"/><Button Content="{local:SkillEditorText RekeyFrames}" Click="RekeyFrames_Click"/><Button Margin="12,0,0,0" Content="{local:SkillEditorText HoldStage}" Click="HoldStage_Click"/><Button Margin="4,0" Content="{local:SkillEditorText ReleaseStage}" Click="ReleaseStage_Click"/><TextBlock Margin="12,0,4,0" VerticalAlignment="Center" Text="{local:SkillEditorText PlaybackSpeed}"/><Slider x:Name="speedSlider" Width="80" Minimum="0.25" Maximum="4" Value="1" ValueChanged="Speed_ValueChanged"/><Slider x:Name="scrubSlider" Margin="8,0" Width="140" Minimum="0" Maximum="1" ValueChanged="Scrub_ValueChanged"/></WrapPanel>
                            <Grid Grid.Row="2"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="250"/></Grid.ColumnDefinitions>
                                <ListBox x:Name="timelineList" ItemsSource="{Binding Frames}" SelectionChanged="Timeline_SelectionChanged" ScrollViewer.HorizontalScrollBarVisibility="Auto" ToolTip="{local:SkillEditorText TimelineClipboardTooltip}" AutomationProperties.Name="{local:SkillEditorText TimelineFrames}"><ListBox.ItemsPanel><ItemsPanelTemplate><VirtualizingStackPanel Orientation="Horizontal"/></ItemsPanelTemplate></ListBox.ItemsPanel><ListBox.ItemTemplate><DataTemplate><Border Margin="2" Padding="4" BorderBrush="{DynamicResource HareBorderBrush}" BorderThickness="1"><StackPanel><Image Source="{Binding Thumbnail}" Width="64" Height="64" Stretch="Uniform"/><TextBlock HorizontalAlignment="Center" Text="{Binding WorkingFrame.Name}"/><TextBlock HorizontalAlignment="Center" Text="{Binding DisplayDelay}"/></StackPanel></Border></DataTemplate></ListBox.ItemTemplate></ListBox>
                                <Grid x:Name="layerInspector" Grid.Column="1" Margin="8,0,0,0"><Grid.ColumnDefinitions><ColumnDefinition Width="90"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><Grid.RowDefinitions><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/></Grid.RowDefinitions>
                                    <TextBlock Text="{local:SkillEditorText OriginX}"/><TextBox Grid.Column="1" Text="{Binding OriginX, UpdateSourceTrigger=LostFocus}"/>
                                    <TextBlock Grid.Row="1" Text="{local:SkillEditorText OriginY}"/><TextBox Grid.Row="1" Grid.Column="1" Text="{Binding OriginY, UpdateSourceTrigger=LostFocus}"/>
                                    <TextBlock Grid.Row="2" Text="{local:SkillEditorText Delay}"/><TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Delay, UpdateSourceTrigger=LostFocus}"/>
                                    <TextBlock Grid.Row="3" Text="{local:SkillEditorText Z}"/><TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Z, UpdateSourceTrigger=LostFocus}"/>
                                    <TextBlock Grid.Row="4" Text="{local:SkillEditorText AlphaStart}"/><TextBox Grid.Row="4" Grid.Column="1" Text="{Binding AlphaStart, UpdateSourceTrigger=LostFocus}"/>
                                    <TextBlock Grid.Row="5" Text="{local:SkillEditorText AlphaEnd}"/><TextBox Grid.Row="5" Grid.Column="1" Text="{Binding AlphaEnd, UpdateSourceTrigger=LostFocus}"/>
                                </Grid>
                            </Grid>
                        </Grid>
                    </Grid></TabItem>

                    <TabItem Header="{local:SkillEditorText Relationships}"><DockPanel Margin="12"><WrapPanel DockPanel.Dock="Top" Margin="0,0,0,8"><Button Content="{local:SkillEditorText NavigateRelationship}" Click="NavigateRelationship_Click"/><Button Margin="4,0" Content="{local:SkillEditorText CopyPropertyToLoaded}" Click="CopyPropertyToLoaded_Click"/><Button x:Name="dragonAssetsButton" Margin="4,0" Content="{local:SkillEditorText DragonAssets}" Click="OpenDragonAssets_Click" Visibility="Collapsed"/></WrapPanel><DataGrid x:Name="relationshipsGrid" AutoGenerateColumns="False" IsReadOnly="True" MouseDoubleClick="Relationship_DoubleClick"><DataGrid.Columns><DataGridTextColumn Header="{local:SkillEditorText RelationshipKind}" Binding="{Binding Kind}"/><DataGridTextColumn Header="{local:SkillEditorText Path}" Binding="{Binding SourcePath}" Width="*"/><DataGridTextColumn Header="{local:SkillEditorText TargetSkill}" Binding="{Binding TargetSkillId}"/><DataGridCheckBoxColumn Header="{local:SkillEditorText Status}" Binding="{Binding Resolved}"/></DataGrid.Columns></DataGrid></DockPanel></TabItem>
                </TabControl>
            </Grid>

            <GridSplitter Grid.Column="3" Width="5" HorizontalAlignment="Stretch" Background="Transparent"/>
            <Border Grid.Column="4" Margin="0,6,6,6" Style="{StaticResource HareCardStyle}"><TabControl x:Name="inspectorTabs">
                <TabItem Header="{local:SkillEditorText KnownProperties}"><ScrollViewer><StackPanel x:Name="knownPropertiesPanel" Margin="8"/></ScrollViewer></TabItem>
                            <TabItem Header="{local:SkillEditorText RawProperties}"><Grid Margin="6"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="2*"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions><WrapPanel><Button Content="{local:SkillEditorText Add}" Click="RawAdd_Click"/><Button Margin="4,0" Content="{local:SkillEditorText Delete}" Style="{StaticResource HareDangerButtonStyle}" Click="RawDelete_Click"/><Button Content="{local:SkillEditorText Rename}" Click="RawRename_Click"/><Button Margin="4,0" Content="{local:SkillEditorText ChangeType}" Click="RawChangeType_Click"/><Button Content="{local:SkillEditorText DuplicateProperty}" Click="RawDuplicate_Click"/><Button Margin="4,0" Content="{local:SkillEditorText CopyPath}" Click="RawCopyPath_Click"/><Button Content="{local:SkillEditorText ImportData}" Click="RawImportData_Click"/><Button Margin="4,0" Content="{local:SkillEditorText ExportData}" Click="RawExportData_Click"/><Button Content="{local:SkillEditorText ResolveLink}" Click="RawResolveLink_Click"/><Button Margin="4,0" Content="{local:SkillEditorText Materialize}" Click="RawMaterialize_Click"/></WrapPanel>
                                <TreeView x:Name="rawTree" Grid.Row="1" Margin="0,8,0,4" ItemsSource="{Binding}" SelectedItemChanged="RawTree_SelectedItemChanged"><TreeView.Resources><HierarchicalDataTemplate DataType="{x:Type local:SkillPropertyNode}" ItemsSource="{Binding Children}"><StackPanel Orientation="Horizontal"><TextBlock Text="{Binding Name}"/><TextBlock Margin="6,0,0,0" Foreground="{DynamicResource HareMutedTextBrush}" Text="{Binding Type}"/></StackPanel></HierarchicalDataTemplate></TreeView.Resources></TreeView>
                                <DataGrid x:Name="rawGrid" Grid.Row="2" Margin="0,4,0,0" AutoGenerateColumns="False" CellEditEnding="Raw_CellEditEnding" SelectionChanged="Raw_SelectionChanged"><DataGrid.Columns><DataGridTextColumn Header="{local:SkillEditorText Property}" Binding="{Binding Path}" IsReadOnly="True" Width="*"/><DataGridTextColumn Header="{local:SkillEditorText Type}" Binding="{Binding Type}" IsReadOnly="True"/><DataGridTextColumn Header="{local:SkillEditorText RawValue}" Binding="{Binding Value, UpdateSourceTrigger=Explicit}" Width="*"/></DataGrid.Columns></DataGrid>
                                <TextBlock x:Name="rawDiagnosticText" Grid.Row="3" Margin="0,6,0,0" Foreground="{DynamicResource HareMutedTextBrush}" TextWrapping="Wrap"/>
                </Grid></TabItem>
                <TabItem Header="{local:SkillEditorText Validation}"><ListBox x:Name="validationList" MouseDoubleClick="Validation_DoubleClick"><ListBox.ItemTemplate><DataTemplate><StackPanel Margin="4"><TextBlock FontWeight="SemiBold" Text="{Binding Message}" TextWrapping="Wrap"/><TextBlock Foreground="{DynamicResource HareMutedTextBrush}" Text="{Binding Path}" TextTrimming="CharacterEllipsis"/></StackPanel></DataTemplate></ListBox.ItemTemplate></ListBox></TabItem>
            </TabControl></Border>
        </Grid>
        <StatusBar Grid.Row="2"><StatusBarItem><TextBlock x:Name="statusText" Text="{local:SkillEditorText Ready}"/></StatusBarItem><Separator/><StatusBarItem><TextBlock x:Name="statusPath"/></StatusBarItem><Separator/><StatusBarItem HorizontalAlignment="Right"><TextBlock x:Name="dirtyText" Foreground="{DynamicResource HareWarningBrush}"/></StatusBarItem></StatusBar>
    </Grid>
</Window>
